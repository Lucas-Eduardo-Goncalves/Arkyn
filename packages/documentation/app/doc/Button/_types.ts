import { ButtonProps } from "@arkyn/types";

type SizeProps = ButtonProps["size"];
type VariantProps = ButtonProps["variant"];
type SchemeProps = ButtonProps["scheme"];

export type { SizeProps, VariantProps, SchemeProps };
